/**
 * Async iterator
 * @callback asyncIterator
 * @param {*} value
 * @param {*} key
 * @param {*} object
 * @returns {Promise}
 */

/**
 * Return the variable itself
 * @param  {*} value
 */
export const identity = (value) => value;
